doctype html
html
  head
    title ToDoList
  body
    h1 ToDoList

    form(method="post")
      input(type="text", name="content")
      button(type="submit") Ajouter
    each task in tasks
      p #{task.content}

       